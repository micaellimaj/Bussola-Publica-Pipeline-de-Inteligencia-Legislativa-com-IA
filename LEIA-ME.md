# Prints (entregáveis da Etapa 5)

Salve aqui as capturas de tela exigidas na entrega. Sugestão de nomes:

- `n8n_execucao_sucesso.png` — workflow n8n executado com todos os nós verdes.
- `n8n_canvas.png` — visão geral do workflow no canvas do n8n.
- `email_digest.png` — e-mail do digest diário recebido.
- `supabase_tema.png` — tabela `fato_proposicoes` no Supabase mostrando as colunas `resumo_executivo` e `tema`.
- `supabase_30dias.png` — evidência de ≥ 30 dias de dados ingeridos.

Depois, referencie esses prints no README principal (há comentários prontos em `docs/README_snippet_Etapa5.md`).
